/** @format */

import styled from 'styled-components';

export const Container = styled.div(() => ({
  display: 'block',
  overflow: 'hidden',
}));

export const Photo = styled.img(() => ({
  width: '100%',
}));
